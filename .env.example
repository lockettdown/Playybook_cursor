# Copy to .env.local for local dev. On Netlify, set the same keys under
# Site configuration → Environment variables, then redeploy (NEXT_PUBLIC_* are baked in at build).

NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_public_key
